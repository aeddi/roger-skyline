ensure A & MX  DNS records

- iptables:
25 (SMTP)
80 (HTTP)
110 (POP3)
143 (IMAP)
443 (HTTPS)
636 (LDAPS)

#ldap: add
dn: uid=postfix,ou=service,dc=slash16,dc=local
changetype: add
objectClass: applicationProcess
objectClass: posixAccount
cn: postfix
uid: postfix
uidNumber: 113
gidNumber: 119
homeDirectory:

#ldappasswd [priviledged bind details] 'uid=postfix,ou=service,dc=slash16,dc=local' -S <<< POSTFIXENTRYPASSWORD

ldap_virtual_{aliases|recipients}.cf
=> bind_dn
=> bind_pw
|| bind = no (?)
# query_filter => user attributes: mail|mailAlias, mailEnabled=TRUE

 - SASL: SMTPS
 - SASL: IMAPS
 - SASL: POP3S
 - (SASL: LDAPS)

 - decide LDAP binding options (+LDAP data & settings)
 - decide LDAP checks ('mailEnabled' attr ? + additional LDAP schemas)
 - dovecot
 - (sasl ?)
 < mail dependend services
    - rsyslog
    - webapp
    - ?



- dovecot keeps filtering with: objectClass=posixAccount, uid=<email>
  asks MUST posixAccount attrs...
  ldap(ffourati@slash16.local): user search: base=ou=people,dc=slash16,dc=local scope=subtree filter=(&(objectClass=posixAccount)(uid=ffourati@slash16.local)) fields=homeDirectory,uidNumber,gidNumber
Despite the explicit config.


Ansible TODO:
  run newaliases
  run postmap on various <FIXME: list> hashtables
